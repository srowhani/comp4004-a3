package server.model.entity.algo;

import server.model.entity.HandEntity;

public class FlushAlgorithm implements Algorithm {
    @Override
    public float calculatePercentile(HandEntity mHand, HandEntity field) {
        return 0;
    }
}
